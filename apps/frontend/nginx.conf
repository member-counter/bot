server {
  listen       80;

  root /usr/share/nginx/html;
  index index.html;

  location / {
      # Serves static assets, then pre-rendered routes (as dirs with index.html),
      # then falls back to the SPA shell for client-side routing.
      try_files $uri $uri/ /200.html;
  }
}